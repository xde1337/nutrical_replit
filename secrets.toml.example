# Streamlit Cloud secrets template
# Create this file in your Streamlit Cloud app settings under "Secrets"

# Optional: Google OAuth (for user authentication)
# Get these from Google Cloud Console
GOOGLE_CLIENT_ID = "your_google_client_id_here"
GOOGLE_CLIENT_SECRET = "your_google_client_secret_here"

# Optional: PostgreSQL Database (for production)
# Get this from your database provider (Railway, Heroku, etc.)
# DATABASE_URL = "postgresql://user:password@host:port/database"

# For demo purposes, leave these commented out to use guest mode and SQLite
