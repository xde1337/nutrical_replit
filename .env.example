# Environment Variables for Deployment
# Copy these to your deployment platform's environment variables section

# Database (if using PostgreSQL)
DATABASE_URL=your_database_url_here

# Google Authentication (if using Google Auth)
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Streamlit Secrets (create this file for local development)
# For production, set these as environment variables

# Deployment Platform Instructions:
# 1. Streamlit Cloud: Add to secrets.toml
# 2. Railway: Add in Variables tab
# 3. Heroku: Use `heroku config:set VAR_NAME=value`
# 4. DigitalOcean: Add in App Settings > Environment Variables
